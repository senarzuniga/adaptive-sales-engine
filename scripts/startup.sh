#!/bin/bash

# Check if the .ai/reports/ directory exists, if not, create it
if [ ! -d "$HOME/.ai/reports/" ]; then
    mkdir -p "$HOME/.ai/reports/"
fi

# Existing startup script content continues here...

