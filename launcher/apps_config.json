{
  "apps": [
    {
      "name": "Next.js Frontend (dev)",
      "path": ".",
      "command": "npm run dev",
      "port": 8080,
      "description": "Vite + React SPA – hot-reload dev server"
    },
    {
      "name": "FastAPI Backend (dev)",
      "path": "backend",
      "command": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
      "port": 8000,
      "description": "Python FastAPI – auto-reload on code changes"
    },
    {
      "name": "Docker Compose (dev stack)",
      "path": ".",
      "command": "docker compose up app-dev",
      "port": 8080,
      "description": "Full dev stack via Docker – frontend hot-reload"
    },
    {
      "name": "Docker Compose (prod stack)",
      "path": ".",
      "command": "docker compose --profile prod up --build app-prod",
      "port": 3000,
      "description": "Production nginx build via Docker"
    }
  ]
}
