# Core HTTP + HTML parsing
requests>=2.32
beautifulsoup4>=4.12

# YAML config
pyyaml>=6.0

# Structured logging
loguru>=0.7

# Supabase client (Python pipeline writes directly to DB)
supabase>=2.4

# OpenAI (LLM extraction — optional, gracefully skipped if absent)
openai>=1.30

# Optional: dynamic / JS-rendered scraping
# playwright>=1.44
